runtime: nodejs8

##################
## Below config is for FLEX environment.  
## Expect to spend $80 / month

# runtime: custom
# env: flex
# manual_scaling:
#   instances: 1
#resources:
#  cpu: .5
#  memory_gb: 0.18
#  disk_size_gb: 10

## gcloud app deploy --version dev
## gcloud app versions stop dev
## gcloud app versions start dev